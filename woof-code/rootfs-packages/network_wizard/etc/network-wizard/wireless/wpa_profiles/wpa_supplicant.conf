ctrl_interface=/var/run/wpa_supplicant
ap_scan=1
update_config=1

# Security: restrict control interface, prefer WPA2+, enable PMF
ctrl_interface_group=0
pmf=1

network={
	ssid="default"
	scan_ssid=1
	psk="defaultpsk"
	proto=RSN WPA
	key_mgmt=WPA-PSK WPA-PSK-SHA256
	pairwise=CCMP
	group=CCMP
}
