ctrl_interface=/var/run/wpa_supplicant
update_config=1

# Security hardening: restrict control interface permissions
ctrl_interface_group=0

# Prefer WPA2/WPA3 over WPA1
# PMF (Protected Management Frames) enabled where supported
pmf=1
